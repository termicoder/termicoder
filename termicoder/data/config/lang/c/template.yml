# you can define custom variables here and use them kind of like
# jinja templates in setting
# specifically row_no can be used while defining editor to open the editor
# at a specific row
row_no: 17
col_no: 23
code: |
  //Author:      YourName
  //Institution: YourInstitute

  #include<stdio.h>

  typedef long long int lli;
  #define loop(i,t) for(i=0;i<t;++i)


  int main()
  {
      lli i,j,t,n;
      scanf("%ld",&t);

      loop(i,t)
      {
          //do something
      }
      return 0;
  }

  //<> with <3 using termicoder:
  //https://termicoder.github.io
